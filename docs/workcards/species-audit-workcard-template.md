# Species Audit Work Card Template

## Header

- `work_id`:
- `owner_workflow`: `species-audit-workflow`
- `linked_workflows`:
- `artifact_type`: `species_audit`
- `status`:

## Scope

- `species`:
- `goal`:
- `audit_kind`:
- `out_of_scope`:

## Mandatory sources

- authority docs:
- authority data:
- species seeds/canon:
- simulator coverage references:
- publication references if relevant:
- knowledge references:

## Phase checkpoints

### Phase 0. Intake
- `audit_scope_recorded`:
- `main_audit_question`:

### Phase 1. Species Baseline
- `species_identity_summary`:
- `expected_mechanical_signature`:
- `required_source_list`:

### Phase 2. Authority Inventory
- `authored_inventory`:
- `role_category_inventory`:
- `obvious_holes_note`:

### Phase 3. Identity Review
- `identity_strengths`:
- `identity_drifts`:
- `outlier_list`:

### Phase 4. Mechanical Spread Review
- `spread_analysis`:
- `redundancy_list`:
- `weak_entries`:
- `dominant_entries`:

### Phase 5. Simulation Coverage Review
- `coverage_matrix`:
- `porting_state_counts`:
- `simulation_support_assessment`:

### Phase 6. Runtime Blocker Review
- `blocker_list`:
- `grouped_blocker_families`:
- `runtime_priority_note`:

### Phase 7. Publication Pressure Review
- `publication_pressure_note`:
- `sync_candidates`:
- `deferred_sync_note`:

### Phase 8. Backlog Prioritization
- `ordered_next_steps`:
- `immediate_next_batch`:
- `deferred_backlog`:

## Audit conclusion

- `identity_status`:
- `coverage_status`:
- `simulation_status`:
- `backlog_priority`:
- `major_blockers`:
- `immediate_next_actions`:

## Acceptance closure

- `species_baseline_recorded`:
- `authored_inventory_checked`:
- `identity_review_done`:
- `mechanical_spread_review_done`:
- `simulation_coverage_review_done`:
- `runtime_blockers_classified`:
- `backlog_prioritized`:
- `pending_items_explicit`:

## Pending items

- `pending`:

## Change impact

- `counts_changed`:
- `coverage_claims_corrected`:
- `newly_recognized_blockers`:
- `follow_up_work`:
