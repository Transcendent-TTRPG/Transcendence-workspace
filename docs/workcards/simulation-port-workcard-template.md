# Simulation Port Work Card Template

## Header

- `work_id`:
- `owner_workflow`: `simulation-port-workflow`
- `linked_workflows`:
- `artifact_type`: `simulation_port`
- `status`:

## Scope

- `target_artifact`:
- `species_or_family`:
- `goal`:
- `current_porting_state`:
- `out_of_scope`:
- `publication_artifacts_in_scope`: no — if corebook entry or technique card are needed, use technique-workflow as owner instead

## Mandatory sources

- authority docs:
- authority data:
- simulator data:
- simulator runtime:
- knowledge references:
- porting-plan references:

## Phase checkpoints

### Phase 0. Intake
- `main_simulation_question`:
- `source_set_confirmed`:

### Phase 1. Authority Read
- `authority_summary`:
- `explicit_mechanical_claims`:
- `explicit_non_claims`:

### Phase 2. Surface Mapping
- `surface_map`:
- `dependency_map`:
- `provisional_gap_classification`:

### Phase 3. Porting Decision
- `port_now_or_not`:
- `final_gap_classification`:
- `split_needed`:

### Phase 4. Data Definition
- `sim_data_strategy`:
- `partial_port_note`:

### Phase 5. Runtime Integration
- `runtime_changes_needed`:
- `shared_surface_or_one_off`:

### Phase 6. Policy Exercise
- `policy_exercisable`:
- `policy_blocker_if_any`:

### Phase 7. Scenario Exposure
- `scenario_tested`:
- `scenario_id_or_gap`:

### Phase 8. Question Definition
- `question_ready`:
- `minimum_cost_question`:
- `cost_question_reference`:
- `derived_question_triggers`:
- `derived_questions`:
- `question_id_or_gap`:

## Integration

- `files_changed`:
- `sim_data_updated`:
- `runtime_updated`:
- `tests_updated`:

## Validation

- `loader_tests_done`:
- `runtime_tests_done`:
- `policy_tests_done`:
- `scenario_checks_done`:
- `question_checks_done`:

## Acceptance closure

- `authority_read_complete`:
- `surface_map_recorded`:
- `gap_classified`:
- `sim_data_updated_or_blocked`:
- `runtime_updated_or_blocked`:
- `coverage_state_updated`:
- `pending_items_explicit`:

## Pending items

- `pending`:

## Change impact

- `runtime_surfaces_touched`:
- `new_reusable_abstractions`:
- `coverage_movement`:
- `follow_up_work`:
